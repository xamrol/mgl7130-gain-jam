<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jam">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JAM">
        <activity
            android:name=".OffersListActivity"
            android:label="@string/title_activity_offers_list"
            android:theme="@style/Theme.JAM.NoActionBar"></activity>
        <activity android:name=".PasswordResetFinalActivity" />
        <activity android:name=".PasswordResetCodeActivity" />
        <activity android:name=".PasswordResetActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SubscriptionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>